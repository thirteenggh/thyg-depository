<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.sonatype.nexus.testdb.example.TestExpectsDAO">

  <insert id="createSchema">
    CREATE TABLE IF NOT EXISTS test_version (
      id INT GENERATED BY DEFAULT AS IDENTITY,
      test_item_id ${UUID_TYPE} NOT NULL,
      version INT NOT NULL,
      CONSTRAINT pk_test_version_id PRIMARY KEY (id),
      CONSTRAINT fk_test_version_item_id FOREIGN KEY (test_item_id) REFERENCES test_item (id)
    );
  </insert>

</mapper>
